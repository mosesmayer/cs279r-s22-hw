import '/imports/api/tasksMethods.tests.js';

// Commenting out auto-generated code below for backup purposes
// import assert from "assert";

// describe("simple-todos-svelte", function () {
//   // Default tests from Meteor
//   it("package.json has correct name", async function () {
//     const { name } = await import("../package.json");
//     assert.strictEqual(name, "simple-todos-svelte");
//   });

//   if (Meteor.isClient) {
//     it("client is not server", function () {
//       assert.strictEqual(Meteor.isServer, false);
//     });
//   }

//   if (Meteor.isServer) {
//     it("server is not client", function () {
//       assert.strictEqual(Meteor.isClient, false);
//     });
//   }
// });
